bin/
obj/
*.db
*.db-shm
*.db-wal
data/
appsettings.*.local.json
.env

# Terraform (note: .terraform.lock.hcl NÃO é ignorado — deve ser versionado)
deploy/terraform/.terraform/
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/terraform.tfvars
deploy/terraform/crash.log

# Build do whatsapp-admin-client, enviado direto pra VM via rsync — não versionado aqui.
deploy/admin-dist/
